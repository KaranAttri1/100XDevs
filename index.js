// Debouncing in JS  => api call should be made after user 
//                      stops for sometime while typing

const getData = () => {
    // calls an api and gets data
    console.log("Fetching data")
}